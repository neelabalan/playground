apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: jenkins_data
    user: jenkins
    secureJsonData:
      password: jenkins123
    jsonData:
      sslmode: disable
      postgresVersion: 1500
    isDefault: true
    editable: true
